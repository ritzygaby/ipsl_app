import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const primary = Color(0xFF6A1B9A); // Purple from Brain/Psi
  static const secondary = Color(0xFF1565C0); // Blue from outlines/lab coat
  static const accent = Color(0xFFD32F2F); // Red from IPSL text
  static const orange = Color(0xFFEF6C00); // Orange from scales
  
  // UI Colors
  static const background = Color(0xFFF8F9FD);
  static const surface = Colors.white;
  static const textPrimary = Color(0xFF2D2D2D);
  static const textSecondary = Color(0xFF757575);
  static const divider = Color(0xFFEEEEEE);
}
